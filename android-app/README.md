# FarmPro Android App (skeleton)

This is a minimal Android app skeleton (Kotlin) to call the FarmPro backend API.

Open the `android-app` directory in Android Studio and build/run the `app` module.

Main files:
- `app/src/main/java/com/farmpro/MainActivity.kt` — sample network call to `/api/info`.

This skeleton is intended for development in Android Studio; it does not include Gradle wrapper binaries here.
