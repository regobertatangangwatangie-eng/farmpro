# FarmPro Frontend

This is a minimal React frontend. Build with `npm run build` to produce static files.

Local dev:

```bash
cd frontend
npm install
npm start
```

To create a production build:

```bash
npm run build
# copy build output into `static/` or let CI handle it
```
